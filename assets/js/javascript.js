function projects() {
    var scroll = document.querySelector("#projects");
    scroll.scrollIntoView({behavior: "smooth", block: "center", inline: "nearest"});
}
function about() {
    var scroll = document.querySelector("#aboutme");
    scroll.scrollIntoView({behavior: "smooth", block: "center", inline: "nearest"});
}
function contact() {
    var scroll = document.querySelector("#contact");
    scroll.scrollIntoView({behavior: "smooth", block: "center", inline: "nearest"});
}
